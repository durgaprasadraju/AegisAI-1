# API Gateway Module
# This module creates an API Gateway REST API

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# TODO: Add API Gateway resource definitions
