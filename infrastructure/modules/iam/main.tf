# IAM Roles and Policies Module
# This module creates IAM roles and policies for services

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

# TODO: Add IAM role and policy resource definitions
