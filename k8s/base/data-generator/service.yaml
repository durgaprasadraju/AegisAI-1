apiVersion: v1
kind: Service
metadata:
  name: data-generator
  labels:
    app: data-generator
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: data-generator
